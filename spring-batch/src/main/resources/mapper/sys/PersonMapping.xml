<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crossyf.dubbo.springbatch.mapper.StudentMapper">
    <resultMap type="com.crossyf.dubbo.springbatch.entity.Student" id="student">
        <result column="id" jdbcType="VARCHAR" property="id"/>
        <result column="firstName" jdbcType="TIMESTAMP" property="firstName"/>
        <result column="lastName" jdbcType="VARCHAR" property="lastName"/>
    </resultMap>

    <insert id="insertBatch">
        insert into student(
            id,
            firstName,
            lastName)
        values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id,jdbcType=VARCHAR},
            #{item.fistName,jdbcType=VARCHAR},
            #{item.lastName,jdbcType=VARCHAR},
            )
        </foreach>
    </insert>

</mapper>